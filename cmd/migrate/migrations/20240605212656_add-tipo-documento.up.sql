CREATE TABLE tipo_documento (
                                id int4 NOT NULL,
                                detalle varchar NOT NULL,
                                CONSTRAINT tipo_documento_pk PRIMARY KEY (id)
);