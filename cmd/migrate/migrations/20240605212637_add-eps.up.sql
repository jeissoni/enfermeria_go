CREATE TABLE eps (
                     nit int4 NOT NULL,
                     nombre varchar NOT NULL,
                     activa bool NULL,
                     CONSTRAINT eps_pk PRIMARY KEY (nit)
);