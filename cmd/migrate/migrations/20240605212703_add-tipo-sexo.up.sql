CREATE TABLE IF NOT EXISTStipo_sexo (
                           id int4 NOT NULL,
                           detalle varchar NOT NULL,
                           CONSTRAINT tipo_sexo_pk PRIMARY KEY (id)
);